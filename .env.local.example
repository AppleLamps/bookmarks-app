# X API (OAuth 2.0) — from https://developer.x.com/en/portal/dashboard
X_CLIENT_ID=
X_CLIENT_SECRET=
X_REDIRECT_URI=http://localhost:3000/api/auth/callback

# Session encryption — generate with: openssl rand -base64 32
SESSION_SECRET=

# Upstash Redis — from https://console.upstash.com
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Stripe — from https://dashboard.stripe.com
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# App URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000
